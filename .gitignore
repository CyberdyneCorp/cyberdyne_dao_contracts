# Foundry
out/
cache/
# Local deploy artifacts (forge script broadcast logs) — never committed.
broadcast/

# Hardhat
artifacts/
cache_hardhat/
typechain-types/
coverage/
coverage.json

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript
*.tsbuildinfo

# SvelteKit / Vite (toy frontend under frontend/)
frontend/.svelte-kit/
frontend/build/
frontend/dist/
frontend/.vercel/
frontend/.netlify/

# Env
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# OS
Thumbs.db

# Local working artifacts (deck exports, generated illustrations, scratch output)
test-results/
deck-assets/

# Deployments (per-run manifests written by DeployCyberdyneDao) — local artifacts.
# Force-add a specific file if you ever want to commit a real network record.
deployments/

# Frontend ABI export (built fresh by `npm run build:abi`; published per-release)
frontend-abi/

# Consolidated addresses (built fresh by `npm run build:addresses`)
addresses.json

# npm pack output
*.tgz

# Logs
logs/
*.log

# Local demo stack runtime pid (written by scripts/demo-up.sh)
.demo-anvil.pid
